# Archived Reference Materials

This directory contains historical design notes, exported documents, and source bundles retained for provenance. These artifacts are not canonical application source and should not be used as build input without review.

The canonical Android implementation lives under `app/`. Current product and acquisition documentation lives directly under `docs/`. Technical implementation notes live under [`../technical/`](../technical/), and extracted source material lives under `source/` at the repository root.
